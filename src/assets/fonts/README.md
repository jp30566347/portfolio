Static TTF instances used to render the CV PDF (`src/cv/CvDocument.tsx`).
Source Sans 3 and Bricolage Grotesque come from Google Fonts, IBM Plex Mono from
IBM's own release (the Google build trips fontkit). All four are under the SIL Open Font
License 1.1. The web pages load the same families through `next/font`.
